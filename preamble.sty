\ProvidesPackage{preamble}

% Encoding & language
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[spanish]{babel}

% Math
\usepackage{amsmath, amssymb, amsthm}
\usepackage{mathtools}

% Algorithms & pseudocode
\usepackage{algorithm}
\usepackage{algpseudocode}

% Figures & tables
\usepackage{graphicx}
\usepackage{float}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{array}

% Color & boxes
\usepackage{xcolor}
\usepackage{tcolorbox}
\tcbuselibrary{theorems, skins, breakable}

% Hyperlinks
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=blue!70!black,
    citecolor=green!50!black,
    urlcolor=blue!80!black,
}

% Page layout
\usepackage{geometry}
\geometry{margin=2.5cm}
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small MATE1102 Matematicas Estructurales — 2026-19}
\fancyhead[R]{\small Carla González}
\fancyfoot[C]{\thepage}

% Code listings
\usepackage{listings}
\lstset{
    basicstyle=\ttfamily\small,
    breaklines=true,
    frame=single,
    numbers=left,
    numberstyle=\tiny\color{gray},
    keywordstyle=\color{blue},
    commentstyle=\color{green!50!black},
    stringstyle=\color{red!70!black},
}

% Theorem environments
\theoremstyle{definition}
\newtheorem{definition}{Definición}[section]
\newtheorem{example}{Ejemplo}[section]
\newtheorem{exercise}{Ejercicio}[section]

\theoremstyle{plain}
\newtheorem{theorem}{Teorema}[section]
\newtheorem{proposition}{Proposición}[section]

\theoremstyle{remark}
\newtheorem{remark}{Nota}[section]

% Custom colored boxes
\newtcolorbox{conceptbox}[1][]{
    colback=blue!5!white, colframe=blue!60!black,
    fonttitle=\bfseries, title=Concepto clave, #1
}
\newtcolorbox{alertbox}[1][]{
    colback=red!5!white, colframe=red!60!black,
    fonttitle=\bfseries, title=Importante, #1
}
\newtcolorbox{notebox}[1][]{
    colback=green!5!white, colframe=green!50!black,
    fonttitle=\bfseries, title=Nota, #1
}
\newtcolorbox{examenbox}[1][]{
    colback=yellow!5!white, colframe=yellow!60!black,
    fonttitle=\bfseries, title=Cae en el examen, #1
}

% Useful commands
\newcommand{\bigO}[1]{\mathcal{O}(#1)}

\usepackage{tikz}
\usetikzlibrary{arrows.meta, shapes.geometric, positioning}
\definecolor{uniandes}{RGB}{0,51,102}      % azul Uniandes
\definecolor{acento}{RGB}{200,16,46}       % rojo acento
\definecolor{grisclaro}{RGB}{245,245,245}
